---
title: Photography
description: Photography
layout: default
---
<section class="page-section" aria-label="Latest Collections">
  <h2>Latest Collections</h2>
  <div class="cards">
    {%- for collection in collections.collection | reverse | limit(4) -%}
    <a class="card" href="{{ collection.url }}" aria-label="{{ collection.data.title }}">
      {% image "./src/images/" + collection.data.img, collection.data.title + " Collection Image", 300 %}
      <h3>{{ collection.data.title }}</h3>
      <p>{{ collection.data.numberofphotos }} Photos</p>
      <span class="button">View</span>
    </a>
    {%- endfor -%}
  </div>
  <a class="button" href="/photography/collections/">All Collections</a>
</section>

<section class="page-section" aria-label="Latest Photos">
  <h2>Latest Photos</h2>
  <div class="gallery" aria-label="Gallery">
    {%- for photo in collections.photo | limit(12) -%}
    <a class="photo" href="/photography/{{ photo.data.id }}/">
      {% image "./src/images/" + photo.data.src , photo.data.alt, 600 %}
    </a>
    {%- endfor -%}
  </div>
</section>
