---
title: Wallpapers
description: Mobile Wallpapers - Free Downloads
id: wallpapers
layout: default.webc
---
<div class="cards">
  {% for wallpaper in collections.wallpaper | reverse %}
  <div class="section split" id="{{ wallpaper.data.id }}" aria-label="{{ wallpaper.data.name }}">
    <img src="/images/{{ wallpaper.data.img }}" alt="{{ wallpaper.data.name }} phone wallpapers" loading="lazy">
    <div class="text">
      <h2 class="h3">{{ wallpaper.data.name }}</h2>
      {% if not wallpaper.data.legacy %}<p>{{ wallpaper.data.date | fullString }}</p>{% endif %}
      <div class="tags">
        {% for tag in wallpaper.data.tags | filterTags %}
          <p class="tag">{{ tag }}</p>
        {% endfor %}
      </div>
      <a class="button" href="/files/{{- wallpaper.data.id -}}.zip">Download</a>
    </div>
  </div>
  <hr>
  {% endfor %}
</div>
