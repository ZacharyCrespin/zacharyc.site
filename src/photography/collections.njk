---
title: Photo Collections
SEOtitle: Collections | Photography
description: All Photo Collections | Photography
layout: 'basic.njk'
id: collections
---
<div class="cards" aria-label="Photo Collections">
  {%- for collection in collections.collection | reverse -%}
  <a class="card" href="{{ collection.url }}" aria-label="{{ collection.data.title }}">
    {% image "./src/images/banners/" + collection.data.id + ".jpg", collection.data.title + " Collection Image" %}
    <h3 class="h4">{{ collection.data.title }}</h3>
    <p>{{ collection.data.numberofphotos }} Photos</p>
    <p>Updated {{ collection.data.date | shortString }}</p>
    <span class="button">View Collection</span>
  </a>
  {%- endfor -%}
</div>