---
title: Photo Collections
description: Photo Collections
id: collections
layout: default.webc
---
<div class="cards small" data-nosnippet>
  {%- for collection in collections.collection | reverse -%}
  <a class="card" href="{{ collection.url }}" aria-label="{{ collection.data.title }} Photo Collection">
    {% image collection.data.img, collection.data.title + " Collection Image", "small", "(max-width: 600px) 100vw, (max-width: 900px) 50vw, (max-width: 1200px) 33vw, 24vw" %}
    <h2 class="h3">{{ collection.data.title }}</h2>
    <p>{{ collection.data.numberofphotos }} Photos</p>
    <p>{{ collection.data.date | fullString }}</p>
    <span class="button">View</span>
  </a>
  {%- endfor -%}
</div>